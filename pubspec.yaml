name: main_auth
description: "This package offers a customizable Login and Sign-Up screen for
  Flutter apps, facilitating integration of forms and social login buttons"

version: 0.0.19
homepage: https://github.com/abdelrahmanghanem
repository: https://github.com/abdelrahmanghanem/main_auth

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.1.0
  main_button: ^0.0.18
  main_text_field: ^0.0.18
  pinput: ^5.0.0
  smart_localize: ^0.0.16


dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:

  assets:
    - assets/images/
